description: 'Invalid request'
content:
  application/json:
    schema:
      type: object
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: 'Bad Request'
        data:
          type: object
          example:
            errors:
              - message: 'Email is required'
